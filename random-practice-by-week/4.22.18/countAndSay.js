function countAndSay(n) {
  // we need to instantiate n
  // current = [ ... ] 
  // some counter ...
  // while counter < n

  // next is formFromList(current)
  // set current to next

}

function formFromList(list) {
  let output = []
  let count = 0;
  let currentChar = null;

  // 1. iterate through the list, but going 'beyond': elem
    // in case we dont have count or currentChar
    // elem matches currentChar
    // does not match ... or 'went over the edge'

  // 2. we can use 'for each'
  //    but then we need to something to the remaining 'current' and 'count'

  return output
}

// tests

console.log(countAndSay([1])) // [ 1, 1 ]
